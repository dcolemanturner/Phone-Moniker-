$(".clear").text("<%= @phone_num.to_s.html_safe %>");
$(".first_result").text("<%= @first_answers.join.to_s.html_safe %>")
$(".second_result").text("<%= @second_answers.join.to_s.html_safe %>")
//first_result